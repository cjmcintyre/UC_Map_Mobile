<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent" android:layout_height="match_parent">


    <ImageView
        android:id="@+id/wimageView"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        app:srcCompat="@mipmap/ic_parking" />

    <TextView
        android:id="@+id/wtViewTitle"
        android:layout_width="60dp"
        android:layout_height="15dp"
        android:layout_alignParentTop="true"
        android:layout_marginStart="12dp"
        android:layout_toEndOf="@+id/wimageView"
        android:text="TextView"
        android:textColor="@color/black"
        android:textSize="12sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/wtViewSnippet"
        android:layout_width="60dp"
        android:layout_height="12dp"
        android:layout_alignStart="@+id/wtViewTitle"
        android:layout_below="@+id/wtViewTitle"
        android:textColor="@color/black"
        android:text="TextView"
        android:textSize="10sp" />
</RelativeLayout>